#include<stdio.h>
#define null '\0'

char* convert(char ch[],int i)
{
    /*
    while(ch[k]!=null)
        k++;

    while(k>i)
        ch[k+7]=ch[k];

    */




}



int main(){
    char ch[180];
    gets(ch);
    puts(convert(ch,strlen(ch)-1));
}
